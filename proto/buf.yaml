version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  ignore_only:
    RPC_REQUEST_STANDARD_NAME:
      - billing/v1/usage_ingestion.proto
    RPC_RESPONSE_STANDARD_NAME:
      - billing/v1/usage_ingestion.proto
breaking:
  use:
    - FILE
